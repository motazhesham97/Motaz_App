{
    "apiUrl": "http://192.168.8.142:8080",
    "apiUrlAndroid": "http://192.168.8.142:8080",
    "runMode": "development",
    "note": "Default desktop and Android API URL. Change these values if the server IP changes. A config.json beside the exe still overrides these defaults."
}
