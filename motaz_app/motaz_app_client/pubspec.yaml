name: motaz_app_client
description: Starting point for a Serverpod client.

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  serverpod_auth_idp_client: 3.4.4
  serverpod_client: 3.4.4

dev_dependencies:
  test: ^1.25.5
  lints: '>=3.0.0 <7.0.0'