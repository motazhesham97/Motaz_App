name: Format

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Install dependencies
        working-directory: motaz_app
        run: flutter pub get

      - name: Check formatting
        working-directory: motaz_app
        run: dart format --set-exit-if-changed motaz_app_client motaz_app_server motaz_app_flutter